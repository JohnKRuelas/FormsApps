import { BasicPageComponent } from './basic-page.component';

describe('BasicPageComponent', () => {
  let component: BasicPageComponent;

  beforeEach(() => {
    component = new BasicPageComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
