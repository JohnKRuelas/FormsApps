import { DynamicPageComponent } from './dynamic-page.component';

describe('DynamicPageComponent', () => {
  let component: DynamicPageComponent;

  beforeEach(() => {
    component = new DynamicPageComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
